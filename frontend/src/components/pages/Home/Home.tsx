import { Box } from "@mui/material";

const Home = () => {
  return (
    <Box
      sx={{
        bgcolor: "#424242",
        height: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      home
    </Box>
  );
};

export default Home;
