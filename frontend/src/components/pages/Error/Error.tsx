import { Box } from '@mui/material';

const Error = () => {
  return (
    <Box
      sx={{
        bgcolor: '#424242',
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      404
    </Box>
  );
};

export default Error;
